<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <h2>Loop</h2>
  <%for (var property in urls) {%>
      <% if (urls.hasOwnProperty(property)) {%>
          <li><%=urls[property] %>  - <%- "<a href =http://localhost:8080/u/"+property+">"%> <%= property%></a>   </li> <%-"<form action=/urls/"+property+"/delete  method = \'post\'>"%>
      <input type='submit' value='DELETE'>
      </form>
      <%-"<form action=/urls/"+property+"/  method =\'post\'>"%>
      <input type='submit' value='EDIT'>
      </form>
      <%}%>
   <%}%>
  <a href= http://localhost:8080/urls/new>Shorten new URL</a>
 
</body>
</html>
