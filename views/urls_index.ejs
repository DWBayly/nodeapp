<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
<% include partials/_header %>
  <h2>Sites</h2>

  <%for (var property in urls) {%>
      <% if (urls.hasOwnProperty(property)) {%>
          <li><%=property %>  - <%- "<a href =http://localhost:8080/u/"+property+">"%> <%= urls[property]%></a>    <%-"<form action=/urls/"+property+"/delete  method = \'post\'>"%>
          <input type='submit' value='DELETE'></form><%-"<form action=/urls/"+encodeURI(property)+"/  method =\'post\'>"%><input type='submit' value='EDIT'></form></li>
      <%}%>
   <%}%>
  <a href= http://localhost:8080/urls/new>Shorten new URL</a>
 
</body>
</html>
